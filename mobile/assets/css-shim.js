// NativeWind CSS shim — styles are processed at build time by the
// babel jsxImportSource:"nativewind" transform. This file is a no-op
// placeholder so Metro can resolve .css imports without crashing.
